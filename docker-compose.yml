version: '3.8'

services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./storage:/app/storage
      - ./logs:/app/logs
    restart: unless-stopped
    environment:
      - DATABASE_URL=sqlite:///./test_task.db
      - STORAGE_PATH=/app/storage
